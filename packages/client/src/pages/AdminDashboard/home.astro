---
import AdminHeader from "../../components/AdminHeader/AdminHeader";
import StudentsTable from "../../components/StudentsTable/StudentsTable";
import Layout from "../../layouts/Layout.astro";
---

<Layout>
    <!-- <div class="admin_dashboard_courses_container">
        <div class="create_course_container">
            <div class="header_title">Courses</div>
            <Button text="Create Course" />
        </div>
        <div class="courses_container">
            <AdminCourseCard courseName="Html" />
        </div>
        <EditCourse client:load showModal={showModal} />
    </div> -->

    <AdminHeader />
    <StudentsTable client:visible />
</Layout>

<style>
    .courses_container {
        display: flex;
        flex-wrap: wrap;
    }
    .course {
        flex: 1;
        min-width: calc(100% / 3);
        max-width: calc(100% / 3);
    }
    .create_course_container {
        display: flex;
        justify-content: space-between;
        padding: 0px 35px 0px 20px;
        /* width: 90%; */
    }
    .create_course_container .header_title {
        font-size: 24px;

        /* width: 90%; */
    }
</style>
